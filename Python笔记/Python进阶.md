# Python进阶

## 函数

### 定义与调用

### 参数传递

- 位置参数调用
- 关键字参数调用

### 默认参数和可变参数

### 局部变量与全局变量

### return语句

### 闭包与嵌套

- 内部函数可以访问外部函数的变量

### 装饰器

-允许在不修改源代码的基础上添加新的函数功能

### lambda

- 用于传递简单函数作为参数

### 递归函数

---

## 模块与包

### 模块

- 概念：.py文件
- 内置模块可以直接使用
- import
- from....import
- import...as
- 自定义模块
- 模块搜索路径
- 常用标准库
- 第三方模块安装与使用

### 包

- 概念：多个模块的目录，用于组织相关模块必须包含一个__init__.py
- `__init__.py`初始化包，识别目录为包并且控制包的导入行为

---

## 文件操作

### 打开与关闭

- `open()`打开，`file.close()`关闭
- 不同的文件打开模式：

        "r": "只读模式（默认）",
        "w": "写入模式（覆盖原内容）",
        "a": "追加模式（在文件末尾添加）",
        "r+": "读写模式（文件必须存在）",
        "w+": "读写模式（覆盖原内容）",
        "a+": "读写模式（在文件末尾添加）"


### 读取方法

### 写入方法

### 文件指针

### with语句

### 文件路径

### csv

### json

---

## 异常处理

## 面向对象

## 高级特性